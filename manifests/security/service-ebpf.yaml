apiVersion: v1
kind: Service
metadata:
  name: ebpf-shield-svc
  namespace: kube-system
  labels:
    app: ebpf-shield
spec:
  selector:
    app: ebpf-shield
  ports:
  - name: metrics
    port: 8080
    targetPort: 8080
