apiVersion: v1
kind: Service
metadata:
  name: nginx-ha-svc
  namespace: default
spec:
  selector:
    app: nginx-ha
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
